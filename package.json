{
  "name": "venguard-itw",
  "version": "1.0.0",
  "scripts": {
    "run": "npx playwright test",
    "run:headed": "npx playwright test --headed",
    "install:browsers": "npx playwright install chromium",
    "github:discover": "node tools/github/discover.js",
    "github:preflight": "node tools/github/preflight.js"
  },
  "dependencies": {
    "dayjs": "^1.11.13"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1"
  }
}
